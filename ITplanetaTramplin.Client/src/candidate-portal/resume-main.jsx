import React from "react";
import ReactDOM from "react-dom/client";
import { CandidateResumeApp } from "./CandidatePortfolioApps";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CandidateResumeApp />
  </React.StrictMode>
);
