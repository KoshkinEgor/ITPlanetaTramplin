import React from "react";
import ReactDOM from "react-dom/client";
import { CandidateOverviewApp } from "./CandidateOverviewApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CandidateOverviewApp />
  </React.StrictMode>
);
